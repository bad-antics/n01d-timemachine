# N01D Time Machine Installation

```bash
git clone https://github.com/bad-antics/n01d-timemachine
cd n01d-timemachine
cargo build --release
```

## Dependencies
- Rust 1.70+, GTK4, libadwaita
- Emulators: vice, fs-uae, fuse, dosbox (install separately)
